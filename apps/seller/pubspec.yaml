name: cnet_store_seller
description: C-Net Store seller application.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0
  flutter_secure_storage: ^9.2.4
  go_router: ^16.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/

