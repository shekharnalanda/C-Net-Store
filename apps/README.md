# C-Net Store apps

The customer, seller and delivery applications are separate Flutter targets sharing the same Laravel REST API contract. Each app must keep independent authentication, bundle identifiers and release pipelines.

