<article class="product-card"><a href="{{ route('products.show', $product) }}"><div class="product-image">@if($product->image_url)<img src="{{ $product->image_url }}" alt="{{ $product->name }}" loading="lazy">@else{{ $product->product_type === 'food' ? '🍲' : ($product->product_type === 'grocery' ? '🥦' : '📦') }}@endif</div><div class="product-info"><small>{{ $product->business->name ?? 'C-Net Store' }}</small><h3>{{ $product->name }}</h3><div class="price"><strong>₹{{ number_format($product->sale_price ?? $product->price, 2) }}</strong>@if($product->sale_price)<del>₹{{ number_format($product->price, 2) }}</del>@endif</div><span class="add">View Details</span></div></a></article>
